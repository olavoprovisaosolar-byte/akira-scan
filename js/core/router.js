/**
 * Bridge — router TypeScript compilado + fallback legado.
 */
export {
    parseRoute,
    validateMangaId,
    clearZone,
    showView,
    buildUrl,
    navigate,
    parseManhwaRoute,
    parseLeitorRoute,
    linkManhwa,
    linkLeitor,
    linkBiblioteca,
    rotaAtual,
    ZONES
} from "../dist/client/core/router.js";
